<p id="notice"><%= notice %></p>

<p>Sort by: <%= link_to "ASC", grades_path(sort_by: "asc") %> <%= link_to "DESC", grades_path(sort_by: "desc") %></p>

<h1>Grades</h1>



</ul>

<table>
  <thead>
    <tr>
      <th>Student ID</th>
      <th>Student name</th>
      <th>Student grade</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @grades.each do |grade| %>
      <tr>
        <td><%= grade.student_id %></td>
        <td><%= grade.student_name %></td>
        <td><%= grade.student_grade %></td>
        <td><%= link_to 'Show', grade %></td>
        <td><%= link_to 'Edit', edit_grade_path(grade) %></td>
        <td><%= button_to 'Destroy', grade, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Grade', new_grade_path %>
